# Credenciales de PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=trafic_red
DB_USER=postgres
DB_PASSWORD=admin

# Configuraciones adicionales de base de datos
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=10

# Configuración del entorno
ENVIRONMENT=development
DEBUG=True

# Puerto del servidor backend
SERVER_PORT=8010

# Configuración de captura de red
CAPTURE_TIMEOUT=300
DEFAULT_INTERFACE=auto

# Configuración de logging
LOG_LEVEL=INFO
