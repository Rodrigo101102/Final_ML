# Backend dependencies for ML Traffic Analyzer
# Versiones compatibles que EVITAN errores de compilación
# Probadas en Python 3.8, 3.9, 3.10, 3.11

# ⚠️ IMPORTANTE: Este proyecto funciona mejor con Python 3.8-3.11
# Si usas Python 3.12+ pueden aparecer errores de compilación

# Dependencias básicas (compatibles con todas las versiones)
fastapi>=0.104.0,<0.110.0
uvicorn[standard]>=0.24.0,<0.30.0
python-dotenv>=1.0.0,<2.0.0
pydantic>=2.5.0,<3.0.0

# Database (siempre usar binary para evitar compilación)
sqlalchemy>=2.0.20,<2.1.0
psycopg2-binary>=2.9.0,<3.0.0

# Machine Learning - VERSIONES ESPECÍFICAS para evitar conflictos
# Estas versiones tienen wheels precompilados para Python 3.8-3.11
scikit-learn==1.3.2
pandas>=2.0.0,<2.2.0
numpy>=1.24.0,<2.0.0
joblib>=1.3.0,<2.0.0

# System utilities
psutil>=5.9.0,<6.0.0

# Dependencias adicionales para evitar errores de compilación
setuptools>=65.0.0
wheel>=0.38.0